node_modules/
.next/
out/

# secrets and local credential store — never commit
.env.local
users.json

# upload scratch space for the deployer
.staging/

# runtime state
*.log
data/bypass-audit.jsonl
.DS_Store
